<Window x:Class="SerialPortTest.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SerialPortTest"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525"
        DataContext="{Binding RelativeSource={RelativeSource Self}}">
    <Grid>
        <StackPanel>
        <TextBox Text="{Binding Input,Mode=TwoWay}" Height="20"/>
        <Button Click="Button_Click" Height="20"/>
        <TextBox Text="{Binding Output,Mode=TwoWay}" Height="20"/>
        </StackPanel>
    </Grid>
</Window>
